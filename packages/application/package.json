{
  "name": "@tasks/application",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "bun run --filter @tasks/domain build && tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@tasks/domain": "file:../domain"
  }
}
